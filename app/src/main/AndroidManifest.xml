<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Dealerproduct_DEActivity"></activity>
        <activity android:name=".Option_DEActivity" />
        <activity android:name=".loginDEActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".loActivity" />
        <activity android:name=".dealerActivity" />
        <activity android:name=".failpaymentActivity" />
        <activity android:name=".thankyouActivity" />
        <activity android:name=".pausesubcriptionActivity" />
        <activity android:name=".PaymentsActivity" />
        <activity android:name=".productshowActivity" />
        <activity android:name=".categoriesActivity" />
        <activity android:name=".placeorderActivity" />
        <activity android:name=".ProfieupdateActivity" />
        <activity android:name=".ProfileDetailsActivity" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".helpActivity" />
        <activity
            android:name=".SignupActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SignUp_DEActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>